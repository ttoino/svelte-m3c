import { IconButton } from "$lib/index.js";

import { visualTest } from "./visual.js";

visualTest(IconButton, {
    icon: "edit",
    variant: ["text", "outlined", "tonal", "filled", "elevated"],
});
